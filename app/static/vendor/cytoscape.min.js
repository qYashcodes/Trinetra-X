/* Placeholder for vendored Cytoscape.js. The fixture canvas uses server SVG until the full asset is added. */
window.cytoscape = window.cytoscape || function () { return { layout() { return { run() {} }; } }; };
