/* Minimal local Alpine-compatible shim for fixture demos. Replace with vendored Alpine.js for production. */
window.Alpine = window.Alpine || { start() {} };
document.addEventListener("DOMContentLoaded", () => window.Alpine.start());
