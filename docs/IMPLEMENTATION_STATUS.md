# TRINETRA Enhanced Features Implementation Status

Baseline: `3943a7a35d3fc07fd0d64d2f7d89a840fd0e1e70` on `main`.

Verification command:

```powershell
C:\Users\Yash\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest -q
```

## v3 additive workflow status (2026-09-20)

| Capability | Level | Evidence and next level |
| --- | --- | --- |
| Validated working context, case scoping and role registry | integration-tested | Server-authoritative context, assignment checks, pre-mount role removal and scoped navigation/workspace state are locally tested. Live verification requires deployment identity and tenant-boundary testing. |
| Event-driven trace presentation and immutable retrace | integration-tested | Persisted SSE/JSON events drive progress; failed/unsupported child attempts remain retained and inactive; completed/incomplete child snapshots preserve parent linkage. Live verification requires a newly executed credentialed retrace. |
| Strategy projection and Omega SVG comparison/export | integration-tested | Engine-enumerated strategies recompute rank, width, emphasis, explanation and projected findings without mutating sealed transfers. Browser exports include strategy, comparison and caption metadata. |
| Six-stage notice workflow and immutable PDF | integration-tested | Parameter validation, attachments, versions, attestation, session lockout, ACP return loop, diffs and Playwright/pypdf A4 validation are locally tested. Legal copy is still pending review. |
| Portal, Email and Nodal-copy delivery | unavailable | Local channel attempts are records only. Approved schemas, credentials, authority and independently verified delivery receipts are required. |
| SAHYOG route | fixture-tested | SAHYOG is an unmistakably simulated/specimen channel and never claims external delivery. Approved integration evidence is required for promotion. |
| CCTNS and SAHYOG re-verification | unavailable | Exact sign-in labels and lockout UI exist; real methods fail closed because no approved authentication integration is configured. |
| DispatchRecord, SLA and case-specific escalation | integration-tested | Shared records, 24-hour/per-VASP SLA, frozen clocks, exactly-once breach outbox events, owner chains and ACP watchers are locally tested. |
| Workflow seed v1 | fixture-tested | Separate idempotent nine-state fixture spans three IOs and two ACPs without modifying canonical trace fixtures. |
| Canonical audit v2 outbox | integration-tested | Legacy JSONL rows remain verifiable; new workflow mutations queue stable event IDs transactionally and flush idempotently. |
| Optional workstation face presence | integration-tested | Two independent server gates default off; session-scoped officer opt-in, same-origin MediaPipe assets, fail-open cleanup, advisory-only motion status and privacy-shield subordination are locally tested. It is face presence only—not identity, authentication or liveness—and requires the deployment hardware/browser acceptance matrix before live verification. |
| Complaint narrative fraud-typology triage | integration-tested | English connector text, searchable PDFs and officer uploads are hashed and stored as append-only protected sources; deterministic scoreless multi-label indicators, explicit unknown/unsupported states, officer review, audit metadata and raw-text-excluding evidence exports are locally tested behind enablement, taxonomy-review and privacy-review gates. Live verification requires an approved real complaint-source integration and reviewed source samples. |

## Phase Status

| Phase | Status | Evidence |
| --- | --- | --- |
| Baseline and status ledger | verified | This document records phase status, touched areas and blocked capabilities; full local suite passed after changes. |
| Identity and session lifecycle | integration-tested | Prototype officer/supervisor login now creates a hashed server-side session record with idle expiry, revocation, activity counters and CSRF-protected sign-out. Active-device listing, sign-out-all, credential-change revocation, audited session summaries, demonstration-marked export identity and responsive idle-obscuring controls are tested locally. Government OIDC/WebAuthn remain unavailable shells, and non-fixture startup fails closed without `SESSION_SECRET`. |
| Live data operation | live-verified | A separate gated live intake accepts exact TRON/USDT address or txid seeds, seals investigator-selected bounds, preserves typed provider failures, displays per-trace mode and data freshness, and retains immutable superseded snapshots. A credentialed bounded trace from the user's network anchored the supplied transaction and first onward hop against TRONSCAN on 2026-09-15. Controlled interruption tests verify visible provider countdown and frontier resume; unconfirmed transfers remain an opt-in observation channel outside attribution, canonical evidence and terminal decisions. |
| Truthfulness boundary | verified | Invalid, unsupported, mismatched and live-disabled traces now close without fixture custody findings. Cache identity prevents stale case snapshots from being reused, including separate live-gated and live-enabled identities. Verified-custody terminals are now the only non-fixture custody terminal allowed to create custody findings, and only when deposit and amount evidence are present. Live edge identity preserves explicit zero and empty provider values instead of truthiness fallbacks. |
| Persistence, stages and UI compatibility | verified | Trace stages include non-custody states; finding IDs are nullable; trace events persist from the engine result; superseding snapshots preserve old evidence; concurrent duplicate trace requests reuse one cache-identity snapshot; no-finding trace pages omit demo custody candidates; no-finding evidence manifests and bundles export with `finding: null` and no custody finding artifact. |
| Evidence model and live TRON slice | live-verified | Trace execution projects canonical events, source coverage, attributed lots, frontier rows and custody assertions in the trace transaction. Every repository or frontier-worker TronGrid response is captured before normalization with an immutable request receipt and schema assessment; non-JSON responses are retained byte-for-byte. Exact per-request source coverage includes query bounds, pagination cursors, provider watermarks, raw hashes and conflicts. A credentialed bounded trace from the user's network stored 22 provider-request hashes, all matched to retained raw payload files, and persisted 24 coverage rows while remaining correctly non-custodial. Deliberate real 403/429 behavior and production custody attribution remain unverified. |
| Allocation, scheduling and resume | integration-tested | Integer proportional allocation, deterministic scheduling, persisted frontier deferrals with original reasons, ancestry-aware cycle deferral, two-victim shared-destination separation, resume-equivalent ordering, resume cursors, local frontier lease/complete/release coordination, bounded live TRON worker cycles, retry/backoff release with queryable coverage telemetry, single-item live TRON resume expansion and per-asset conservation coverage are tested. A feature-gated supervised worker recovers stale leases and drains persisted frontier work with aggregate key-free telemetry. Reaching `live-verified` still requires observing a long-running worker recover a real interrupted provider request from the user's deployment network. |
| Transparent feature extraction and risk check | live-verified | The authenticated fixture/live risk-check paths now expose ten deterministic raw feature families with transaction evidence references, versioned evidence-band rules, first-class sparse-history `unknown`, separate attribution-source provenance, append-only lookup-result records and audited check/escalation actions. A credentialed three-day TronGrid lookup from the user's network on 2026-09-15 computed 1,305 confirmed events, retained seven valid provider receipts and sealed 10,499 feature evidence references. Score and posterior remain null, probability remains disabled, calibration status is visible, and burner classification remains disabled pending an independently reviewed threshold and false-positive benchmark. |
| Explainability surfaces | integration-tested | Trace seeds, hops, edges, parked branches, terminals, bounds, frontier counts and summary values open a two-register explanation drawer. The investigator register is the default; the technical register exposes recorded integer arithmetic, residual carry, scheduling rank, sibling deferrals and transaction/provider evidence identities. Legacy snapshots explicitly report missing scheduling or allocation records rather than reconstructing them. Evidence bundles contain JSON and text methodology annexes, and notice/SAHYOG specimen exports carry the same methodology and limitations. Desktop, mobile and printable surfaces were visually verified locally. A fresh credentialed live trace with the new per-event provider linkage was not run in this stage. |
| Custody and action workflow | partially verified | Custody/action storage contracts, certified provider response bundles, labelled provider ledger/KYC/trade/withdrawal/session rows, duplicate import conflict checks, shared-hot-wallet/deposit separation, verified-custody terminal provenance, import validation, plan-named action-state transitions and bounded confirmed-outcome evidence are tested locally. SAHYOG export is authenticated and specimen-only with an explicit integration-pending boundary. Notice dispatch is rendered as pending/amber rather than a success or confirmed-restraint state. Live provider imports and real action routing remain blocked. |
| Later capability slices | partially verified | CCTP/swap/correlation/boundary contracts, including non-negative CCTP output conservation, and privacy/review claim checks are tested locally. The multi-chain router contract is integration-tested for seed routing, ambiguous-match failure and ETH/BTC fail-closed terminals. EVM, Bitcoin, live bridge/DeFi decoders and calibrated probabilities remain unavailable with explicit gates. |
| Rollout hardening | partially verified | Capability matrix and feature flags expose disabled live/provider/protocol paths, distinguish the credentialed schema/pagination verification from the gated bounded live TRON trace and supervised worker, and show capabilities as enabled only when their independent gates pass. Keys alone do not enable live/provider work without schema, smoke-test, trace-verification, worker, authority or approval gates. Database uniqueness guards prevent duplicate canonical evidence, frontier work, custody assertions and protocol links. |

## Files Changed In This Pass

- `app/data/narrative_typology_v1.json`, `app/services/narrative_triage.py`: versioned English seven-typology indicator rules, Unicode-preserving deterministic matching, negation and overlap handling, fail-closed PDF extraction, content-addressed source storage, idempotent assessment persistence and append-only officer review.
- `app/models.py`, `app/main.py`, `app/integrations/complaints/base.py`, `app/integrations/complaints/fixture.py`: complaint narrative, assessment and review records; optional connector text plus complaint-PDF ingestion; authenticated officer upload/source/review routes; case-level presentation data and metadata-only evidence export. Narrative outcomes do not alter case category, trace execution or custody decisions.
- `.env.example`, `app/services/feature_flags.py`, `app/services/capabilities.py`, `app/services/integrations.py`: default-off narrative enablement, taxonomy-review and privacy-review gates with honest readiness reporting.
- `app/templates/_narrative_triage.html`, `app/static/narrative-triage.css`, `app/templates/trace.html`, `app/templates/live_trace.html`: shared responsive narrative source, indicator, negated-mention, officer-review and upload panel with explicit investigative-aid boundaries.
- `tests/test_narrative_triage.py`: empty/generic/non-English/negated regressions, all seven typologies, multi-label and longest-match behavior, searchable PDF handling, persistence/supersession/review, connector separation, authenticated routes, privacy and evidence-bundle coverage.
- `.env.example`, `app/settings.py`: required non-fixture session-secret configuration plus explicit cookie and idle-timing settings; no static development secret fallback remains.
- `app/models.py`, `app/services/sessions.py`: hashed server-side officer sessions, expiry/revocation, device summaries, activity counters and sign-out summaries.
- `app/main.py`: session resolution, audited logout/logout-all, active-session routes, case/trace/export accounting, credential-change revocation and authenticated artifact identity/audit references.
- `app/templates/login.html`, `app/templates/base.html`, `app/templates/_workspace_nav.html`, `app/templates/_session_controls.html`, `app/templates/sessions.html`, `app/static/app.css`, `app/static/app.js`: visibly labelled demonstration login, integration-pending SSO links, officer top bar, idle shield/warning/extend flow, responsive session management and sign-out controls.
- `app/services/integrations.py`, `app/services/capabilities.py`, `app/providers/webauthn.py`: honest identity/session capability reporting with local TLS, secret-vault, tenant-boundary, monitoring and backup gaps kept visible; WebAuthn remains integration-pending.
- `tests/test_session_lifecycle.py`: focused coverage for secret gating, token hashing, expiry, activity counts, export identity, CSRF sign-out, multi-device revocation and honest integration posture.
- `.env.example`, `app/settings.py`, `app/services/feature_flags.py`, `app/services/capabilities.py`, `app/services/integrations.py`: default-off workstation-presence timing, enablement/privacy-review gates and honest capability/integration reporting.
- `app/main.py`, `app/templates/base.html`, `app/templates/_session_controls.html`, `app/static/app.js`, `app/static/app.css`: CSRF-protected session preference, camera permissions policy, same-origin connection boundary, conditional accessible controls and a restricted one-way session-shield controller.
- `app/static/presence-controller.mjs`, `app/static/presence-runtime.mjs`, `app/static/presence-detection.js`: testable face-presence state machine, GPU-to-CPU detector fallback, advisory luminance motion, camera lifecycle cleanup and authenticated-page bootstrap with no preview or frame transfer.
- `app/static/vendor/mediapipe/`: pinned MediaPipe Tasks Vision 1.0.1 runtime, BlazeFace short-range model, SHA-256 provenance and complete Apache-2.0 distribution notices.
- `tests/test_workstation_presence.py`, `tests/test_presence_controller.py`, `tests/js/test_presence_controller.mjs`, `tests/test_presence_assets.py`: gate/session/header/UI regressions, browser-controller lifecycle scenarios and a real same-origin MediaPipe initialization smoke test that rejects external requests.
- `.env.example`, `app/settings.py`: bounded live risk-history lookback configuration.
- `app/services/behavior.py`: deterministic integer feature extraction, evidence references, sparse-history gating, versioned evidence-band rules and an explicitly disabled burner class.
- `app/services/risk.py`, `app/services/evidence_store.py`: gated live TRON history lookup, case-less append-only provider evidence and result records, separate attribution-source lines, fail-closed provider outcomes and null/disabled model outputs.
- `app/main.py`: authenticated and audited fixture/live risk routes, local case-history linkage, blocked-gate records and explicit case-intake preparation without automatic case creation.
- `app/templates/risk.html`, `app/static/risk.css`: fixture/live mode control, raw feature and evidence-reference rows, visible calibration/method boundaries, source provenance and responsive case escalation.
- `tests/test_behavioral_risk.py`: sparse, zero-value, deterministic feature, disabled-burner, case-less evidence, provider failure, source separation, API/UI audit and intake-handoff coverage.
- `app/services/explainability.py`, `app/engine_bridge.py`, `app/repository.py`, `engine/adapters/tron.py`: two-register trace explanations, methodology annex generation, exact live allocation/scheduling decisions and transaction/provider evidence identity carried into persisted snapshots.
- `app/main.py`, `app/services/notices.py`: authenticated explanation API plus methodology-bearing evidence bundles, manifests and specimen SAHYOG exports.
- `app/templates/_trace_explainability.html`, `app/templates/trace.html`, `app/templates/live_trace.html`, `app/templates/canvas.html`, `app/templates/live_canvas.html`, `app/templates/notice.html`, `app/templates/notice_document.html`, `app/static/explainability.css`, `app/static/explainability.js`, `app/static/canvas.css`, `app/static/canvas.js`, `app/static/live.css`, `app/static/notice.css`: investigator/technical drawer controls, transaction-level trace targets, responsive overflow handling and printable methodology annex presentation.
- `tests/test_trace_explainability.py`: explanation-question, integer arithmetic, scheduling, evidence identity, terminal gate, UI register and export-annex coverage.
- `.env.example`, `app/services/feature_flags.py`, `app/services/capabilities.py`, `app/services/integrations.py`: explicit supervised-worker gate, bounded worker settings, capability posture and aggregate key-free telemetry.
- `app/main.py`, `app/services/worker.py`: application-lifespan supervision, graceful stop, stale-lease recovery and bounded persisted-frontier draining.
- `app/repository.py`, `app/services/evidence_store.py`: automatic pre-normalization provider capture, immutable request/schema receipts, byte-exact non-JSON artifacts and per-request source-coverage linkage.
- `app/services/frontier.py`, `app/services/live_tron_resume.py`: stale-lease recovery history and provider-directed retry delays while retaining failure evidence.
- `engine/adapters/tron.py`: confirmed-only requests, typed schema/rate-limit failures, Retry-After handling, strict response validation and canonical TRON-address conversion for event payloads.
- `tests/test_provider_worker.py`: known-real normalization, exact event/address handling, pagination evidence, schema drift, repository linkage, retry delay, stale recovery and supervised-worker regression coverage.
- `app/engine_bridge.py`, `engine/adapters/tron.py`: exact address/txid live seed resolution, chronology anchoring, bounded confirmed traversal, opt-in unconfirmed observations, typed provider failures, retryable partial paths and provider retrieval timestamps.
- `app/main.py`, `app/repository.py`, `app/services/live_trace.py`, `app/services/live_tron_resume.py`, `app/services/audit.py`, `app/services/feature_flags.py`: separate gated live intake, sealed per-trace bounds, immutable force-retrace supersession, audit history, live status polling, plain-language deferrals, freshness/provenance attachment and exact-parameter resume.
- `app/templates/_trace_mode_banner.html`, `app/templates/live_intake.html`, `app/templates/live_trace.html`, `app/templates/live_canvas.html`, `app/static/live.css`, `app/static/live-trace.js`: unmistakable fixture/live mode treatment, responsive live workflow, per-node stale state, provider countdown and confirmed-only canvas.
- `app/templates/case_intake.html`, `app/templates/docket.html`, `app/templates/trace.html`, `app/templates/canvas.html`, `app/templates/finding.html`, `app/templates/notice.html`, `app/templates/notice_document.html`, `app/templates/_workspace_nav.html`, `app/static/app.css`, `app/static/trace.css`, `app/static/finding.css`: mode visibility and trace provenance across existing screens and notice presentation.
- `app/services/exhibit.py`, `app/services/notices.py`: trace mode, active bounds and data freshness in evidence exhibits and notice/export metadata without synthesizing a live custody endpoint.
- `tests/test_live_data_operation.py`: exact seed resolution, typed network failure, unconfirmed isolation, persisted countdown/resume, immutable retrace, bound parsing, case/snapshot isolation and live UI/API coverage.
- `app/engine_bridge.py`: typed trace result boundary, fixture seed validation, non-custody terminal serialization, wrong-chain VASP guard, verified-custody finding gate, gated live TRON seed verification, gated bounded multi-hop live TRON traversal, one-hop live outgoing frontier projection and live-gate-aware cache identities.
- `app/repository.py`: nullable finding persistence, atomic trace write, case-stage mapping, engine event storage, cache-identity reuse, verified source coverage, canonical evidence projection, provider event-index persistence, verified-custody terminal provenance, queued/deferred live frontier storage and original deferral-reason preservation.
- `app/models.py`, `app/db.py`: non-custody case stages, additive evidence/frontier/custody/action/provider-response tables, trace cache-identity indexing, evidence uniqueness indexes and idempotent local SQLite upgrades for existing trace snapshots.
- `app/main.py`, `app/services/notices.py`, `app/templates/trace.html`, `app/templates/risk.html`, `app/services/risk.py`, `app/static/trace.js`, `app/static/app.css`, `app/static/notice.css`, `app/static/risk.css`: nullable finding and terminal-only trace rendering, server-rendered no-finding custody-candidate suppression, no-finding evidence export compatibility, authenticated SAHYOG specimen export, clean missing-finding handling, pending-state notice dispatch presentation and scoreless fixture evidence-band risk presentation.
- `app/services/exhibit.py`: evidence SVG rendering for snapshots with no hops.
- `app/services/allocation.py`, `app/services/frontier.py`: integer proportional allocation, deterministic scheduling helpers, persisted frontier resume/deferral rows, repeated-arrival separation and local lease/complete/release coordination.
- `app/services/live_tron_resume.py`: leased TRON frontier expansion with canonical events, child lots, queued/deferred child frontier, ancestry-aware cycle deferral, stationary remainder, provider coverage, bounded worker cycles, provider-backoff retry release and provider-backoff source coverage telemetry.
- `app/services/accounting.py`: per-asset conservation reporting and persisted conservation coverage for snapshot value outcomes.
- `engine/adapters/tron.py`: TronGrid V1 contract methods for TRC-20 transfer history, pagination and seed Transfer-event verification.
- `app/services/evidence_store.py`: canonical raw-evidence persistence with retrieval metadata and SHA-256.
- `app/services/custody.py`: custody import, stricter duplicate-conflict validation, certified provider response row persistence, plan-named action workflow contracts and confirmed-outcome evidence/amount bounds.
- `app/services/review.py`: evidence-class, privacy-boundary and claim-reference validation helpers.
- `engine/adapters/evm.py`, `engine/adapters/btc.py`: explicit unavailable adapter status and typed errors for unsupported chains.
- `app/services/protocols.py`: CCTP, swap, correlation and unknown-operation evidence contracts with idempotent duplicate recording and CCTP fee bounds.
- `app/services/capabilities.py`, `app/services/integrations.py`: capability matrix exposed in integration status, including separate TRON provider-contract, live seed-verification and bounded live-trace rows.
- `app/services/feature_flags.py`: explicit rollout gates for live TRON seed verification, bounded live TRON tracing, provider imports, protocol decoders and privacy review; credentials alone do not enable live work.
- `TODO_ENGINE.md`: live-work backlog updated so completed offline TRON pagination contracts are not listed as missing.
- `tests/test_engine_truthfulness.py`, `tests/test_allocation_and_scheduling.py`, `tests/test_accounting_conservation.py`, `tests/test_core_contracts.py`, `tests/test_db_upgrade.py`, `tests/test_evidence_store.py`, `tests/test_app_smoke.py`, `tests/test_finding_notice_integrity.py`, `tests/test_frontend_integrity.py`, `tests/test_provider_custody_review_contracts.py`, `tests/test_protocol_boundaries.py`, `tests/test_feature_flags.py`: focused regression coverage.

## Latest Local Verification

- `C:\Users\Yash\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest -q --basetemp=tmp\pytest-narrative-full-01` passed on 2026-09-21 with `210 passed` and one pre-existing Starlette/AnyIO deprecation warning. This narrative-triage implementation started from a `197 passed` baseline using a workspace-local pytest temp directory because the default Windows pytest temp root was inaccessible.

## Blocked Capabilities

- Government identity and production session deployment: OIDC/WebAuthn remain unavailable pending approved provider metadata, credentials, authority configuration and a secure relying-party origin. Local HTTP has no TLS termination, secret vault, production tenant boundary, monitoring or backup integration. Non-fixture startup now requires `SESSION_SECRET`; secure cookies require TLS before `TRINETRA_SESSION_COOKIE_SECURE=true` can be enabled.
- Production custody findings from live TRON/USDT traces: blocked until provider-certified attribution/ledger schemas, authority and production response samples are available. The now live-verified bounded trace remains no-custody and closes as incomplete/stationary/trail-dissipated/depth-exhausted until those separate custody gates pass.
- Production supervised frontier draining: the worker is feature-gated and integration-tested, but real long-running queue drain, process recovery and provider rate-limit recovery have not been observed in this environment. Enabling it also requires all live-TRON verification gates and the independent worker flag.
- EVM and Bitcoin tracing: unsupported until adapter gates, evidence identity and allocation rules are implemented.
- Provider custody imports: storage contracts are local only until authenticated schemas and authorisation are configured.
- Legal or provider action submission: specimen workflow only; SAHYOG export stays integration-pending and no production dispatch, freeze, seizure or KYC request is authorised.
- Live bridge and DeFi decoders: local evidence contracts are tested, but no live protocol decoder is enabled.
- Live sanctions, reported-abuse and reviewed VASP-registry lookups: unavailable until approved source connectors, provenance schemas and current source access are configured. The risk check reports each missing source independently and does not substitute fixture attribution into live results.
- Live verification of per-event explanations: the explanation contract and UI are integration-tested against retained canonical evidence and provider-shaped fixtures. Promotion to live-verified requires a fresh credentialed trace created after this stage, followed by comparison of its allocation arithmetic, pending ranking, transaction identity and raw provider hashes with the retained provider responses and an independent explorer.
- Workstation-presence live verification: automated controller and real local-asset initialization tests pass, but the capability remains default-off and not live-verified until Chrome and Edge are exercised on the intended localhost/HTTPS deployment with permission allow/deny, GPU/CPU, real walk-away timing, tab restore, camera unplug, navigation and disable-indicator checks.

## Provider Documentation Checked

- TRON Developer Center API overview and TronGrid V1 references were checked on 2026-09-13 and rechecked on 2026-09-15 while adding the provider contract, exact response evidence and bounded traversal:
  - https://developers.tron.network/docs/api
  - https://developers.tron.network/reference/trongrid-v1-api-overview
  - https://developers.tron.network/reference/get-trc20-transaction-info-by-account-address
  - https://developers.tron.network/reference/get-events-by-transaction-id
  - https://developers.tron.network/reference/rate-limits
  - https://developers.tron.network/docs/get-trc20-transaction-history
  - https://developers.tron.network/docs/trongrid
  - https://github.com/TRON-US/trongrid
- The implemented request shapes use `GET /v1/accounts/{address}/transactions/trc20` for TRC-20 history and `GET /v1/transactions/{txid}/events` for event verification. A credentialed bounded trace from the user's network verified the supplied seed and first onward transfer against their TRONSCAN transaction pages on 2026-09-15. The retained verification snapshot is incomplete, not custodial: a high-activity onward branch reached the configured provider page boundary and remains deferred with its persisted retry time. Bounded live traversal requires `TRONGRID_API_KEY`, schema/smoke gates and `TRINETRA_LIVE_TRON_TRACE_VERIFIED=true`; the supervised worker additionally requires `TRINETRA_ENABLE_FRONTIER_WORKER=true`. The separate TRONSCAN API returned HTTP 429 during independent lookup, deliberate provider-error injection was not performed, and custody findings still require separate provider-attribution gates.
- Circle CCTP and Uniswap v4 documentation were checked on 2026-09-13 while adding protocol evidence contracts. The local contracts model CCTP burn/message/attestation/destination execution and Uniswap output-recipient semantics without enabling live decoding.

## Current Rollback Action

Disable new trace execution by leaving `TRINETRA_MODE=fixture`; unsupported and mismatched seeds will still fail closed instead of falling back to demo custody.
