"""Chain-specific trace runtime modules.

The public application facade remains ``app.engine_bridge``.  Modules in this
package hold router and per-chain live runtime code so new chains can be added
without growing the facade further.
"""

